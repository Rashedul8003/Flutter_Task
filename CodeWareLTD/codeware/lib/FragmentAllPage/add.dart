import 'package:flutter/material.dart';

class Add_ extends StatelessWidget {
  const Add_({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Add'),
      ),
    );
  }
}
