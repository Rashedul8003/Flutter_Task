import 'package:flutter/material.dart';

class AcUnit extends StatelessWidget {
  const AcUnit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ac unit'),
      ),
    );
  }
}
